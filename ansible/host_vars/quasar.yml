ORBIT_HOME: "{{ lookup('env','HOME') }}/.orbit"  # runtime (sourced)
ORBIT_DEV_HOME: "{{ lookup('env','HOME') }}/Library/CloudStorage/Dropbox/matrix/orbit"  # dev (Dropbox)
ORBIT_REMOTE: "https://github.com/suhailphotos/orbit.git"
ORBIT_BRANCH: "main"

brew_taps_extra:
  - epk/epk

brew_casks_extra:
  - font-sf-mono-nerd-font

desktop_casks_extra: []
desktop_casks_skip: []

# Include machine specfic apps like this:
#desktop_casks_extra:
#  - blender
#desktop_casks_skip:
#  - inkscape
