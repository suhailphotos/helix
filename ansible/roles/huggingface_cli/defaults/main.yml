---
hf_cli_enabled: false
hf_cli_package: "hf"
hf_cli_version: "latest"
hf_cli_upgrade: true

# Linux: which user owns the uv tools
hf_cli_user: "{{ ansible_user | default(ansible_facts['user_id']) }}"
hf_cli_home: "{{ ansible_facts['user_dir'] }}"
